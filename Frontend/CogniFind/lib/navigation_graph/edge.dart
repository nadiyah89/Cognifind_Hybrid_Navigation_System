class NavEdge {
  final String fromId;
  final String toId;
  final double distanceMeters;

  const NavEdge({
    required this.fromId,
    required this.toId,
    required this.distanceMeters,
  });
}

